= Флаг

Элемент управления `_Флаг_` предназначен для просмотра и изменения булевых данных (да/нет) карточки.

include::partial$properties.adoc[tags=dependsOn]

include::partial$editModes.adoc[tags=edit]
+
.Элемент управления "Флаг" в режиме редактирования
image::checkbox-edit-mode.png[Элемент управления "Флаг" в режиме редактирования]
+
include::partial$editModes.adoc[tags=editInPlace]
+
В карточке выглядит аналогично режиму редактирования.
+
include::partial$editModes.adoc[tags=noEdit]
+
.Элемент управления "Флаг" в режиме чтения
image::checkbox-show-mode.png[Элемент управления "Флаг" в режиме чтения]
+
В зависимости от настроек, ЭУ в режиме чтения также может содержать иконку, соответствующую значению, и собственный текст для значения.
+
image::checkbox-with-icon-show-mode.png[Элемент управления "Флаг" в режиме чтения с выбранной иконкой]

== Свойства

include::partial$properties.adoc[tags=general]

=== Внешний вид

include::partial$properties.adoc[tags=hint]

==== Рисунок для значения "да"

Иконка, отображаемая в элементе управления при просмотре карточки, если флаг установлен. Иконка отображается слева от текста значения *да*.

Выбор иконки осуществляется при нажатии кнопки *…* в данном поле.

.Окно выбора файла изображения для рисунка значения
image::checkbox-select-image.png[Окно выбора файла изображения для рисунка значения]

Поддерживаются изображения следующих форматов: `.jpeg`, `.png`, `.bmp`, `.gif` (без анимации), `.tiff`, `.ico`, `.jxr`. Рекомендуемый размер изображения: 28 пикселей в высоту.

==== Рисунок для значения "нет"

Иконка, отображаемая в элементе управления при просмотре карточки, если флаг установлен. Иконка отображается слева от текста значения *нет*.

Выбор рисунка осуществляется аналогично выбору рисунка для значения *да*.

==== Скрыть поле для

Определяет значение элемента управления, при котором он должен быть скрыт:

* *_Значение не указано_*. Если значение отсутствует.
* *_Да_*. Если в значении *_true_*.
* *_Нет_*. Если в значении *_false_*.
* *_Не скрывать_*. Отображение элемента управления не зависит от значения).

==== Текст для значения "да"

Текст, отображаемый в режиме *_Без редактирования_*, если флаг установлен.

`*Локализуемый*`

==== Текст для значения "нет"

Текст, отображаемый в режиме редактирования *_Без редактирования_*, если флаг не установлен.

`*Локализуемый*`

include::partial$properties.adoc[tags=labelText]

=== Данные

include::partial$properties.adoc[tags=extendedDataSource]

include::partial$properties.adoc[tags=dataSource]

==== Поле данных

Булево поле карточки, с которым связывается элемент управления.

==== Операция редактирования

Выбор операции для редактирования значения элемента управления. Если операция недоступна пользователю, то изменения списка ссылок или добавления комментария к ссылке будет недоступно. Если операция не выбрана, то возможность редактирования значения элемента управления не проверяется.

Если значение `*Операции редактирования*` наследуется от родительского Блока, название свойства меняется на `*Операция редактирования (наследовано)*`.

=== Поведение

include::partial$properties.adoc[tags=visibility]

include::partial$properties.adoc[tags=addCss]

include::partial$properties.adoc[tags=editOperationForVisibility]

include::partial$properties.adoc[tags=disabled]

==== Значение по умолчанию

Значение ЭУ, устанавливаемое по умолчанию при создании карточки.

* Когда флаг установлен, флаг в ЭУ также установлен.
* Когда флаг не установлен, флаг в ЭУ по умолчанию снят.

include::partial$properties.adoc[tags=editMode]

include::partial$properties.adoc[tags=stdCss]

=== События

include::partial$properties.adoc[tags=mouseOver]

include::partial$properties.adoc[tags=mouseAway]

include::partial$properties.adoc[tags=dataChanged]

include::partial$properties.adoc[tags=onClick]
