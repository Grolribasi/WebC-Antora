= Ошибки, исправленные в текущей версии

include::admin:partial$table.adoc[]
|WebC-11130 (SUP-5392)
|Возникала ошибка, когда настроенное условие выбора разметки делегирования в карточке задания не срабатывало.

|WebC-11062 (SUP-5347)
|Блок с файлом отчёта по умолчанию отображался свёрнутым, хотя в настройках ЭУ флаг `*Отображать свёрнутым*` не был установлен.

|WebC-11058 (SUP-5230)
|Возникала ошибка `Последовательность не содержит элементов` при попытке отправить задания на делегирование.

|WebC-11051 (SUP-5298)
|При открытии любого задания из папки "Мои задания" или из почтового уведомления появляется ошибка.

|WebC-11046 (SUP-5350)
|Исправлена ошибка, когда при подписании документа простой подписью, у подписи добавлялось лишнее поле _За кого_ и фамилия подписанта.

|WebC-11043 (SUP-5342)
|Исправлена ошибка, когда после консолидации в задании на согласование отсутствовали файлы.

|WebC-11022 (SUP-5329)
|Если в ЭУ `_Выбор папки_` выбор папок был ограничен, создать карточку в разрешённой папки было невозможно: `Выбрать папку (в текущей запрещено)`.

|WebC-11012 (SUP-5316)
|Некорректно отображался ЭУ для выбора исполнителей в мобильной версии с телефона.

|WebC-11007 (SUP-5304)
|В ЭУ `_Записи справочника сотрудников_` некорректно работала сортировка в выпадающем списке быстрого поиска.

|WebC-11002 (SUP-5296)
|Если в карточке документа была создана таблица и выполнено промежуточное сохранение, при сохранении документа возникала ошибка `Item not found`.

|WebC-10984 (SUP-5279)
|При создании Исходящего документа в ответ на Входящий в карточке созданного документа ссылка дублировалась из-за того, что карточка Исходящего сохранялась перед созданием.

|WebC-10974 (SUP-5261)
|В Справочнике сотрудников в выбранной должности отображались сотрудники из ранее выбранной должности.

|WebC-10973 (SUP-5255)
|При попытке открыть файл, для которого не поддерживается предварительный просмотр, в справке по ЭУ `_Дерево исполнения_` появлялось сообщение `Карточка не содержит основных файлов, поддерживающих предпросмотр` вместо скачивания.

|WebC-10948 (SUP-5248)
|Папки в элементе управления `_Папка / Известная папка_` сортировались в первую очередь по наличию подпапок и только потом -- по алфавиту.

|WebC-10944 (SUP-5247)
|Возникала ошибка в ЭУ `_Нумератор_`: в режиме редактирования по месту новый номер назначался, если выделить текущий номер и отпустить кнопку мышки.

|WebC-10940 (SUP-5240)
|При завершении задания согласования с отрицательным результатом открывалась разметка с полем `Комментарий` без возможности многострочного ввода.

|WebC-10932 (SUP-5230)
|При делегировании задания на исполнение, поле для добавления комментария пропадало, когда курсор выделения текста выходил за границы поля.

|WebC-10913 (SUP-5218)
|Возникала ошибка `Value cannot be null. Parameter name: value` при нажатии кнопок *Вверх* и *Вниз* для навигации по списку справочных значений.

|WebC-10907 (SUP-5174)
|У пользователей в папке _Мои задания_ не отображались новые задания

|WebC-10904 (SUP-5206)
|Поля в форме для добавления связанной карточки не очищались при нажатии кнопки *Очистить все*.

|WebC-10875
|Отсутствовала кнопка *X* для очистки поисковой строки по справочнику от введённого значения.

|WebC-10523 (SUP-5004)
|При открытии параметрического поиска в Safari на iPhone клавиатура заслоняла поле ввода.

|WebC-10466 (SUP-4911)
|Исправлена ошибка, при которой во время предварительного просмотра `.pdf` у страницы документа изменялся масштаб и ориентация, если в документе имелись страницы с альбомной ориентацией.

|WebC-10200 (SUP-4781)
|Возникала ошибка при выполнении пользовательского скрипта: метод `await SystemJS.import("@docsvisin/webclient/System/getAbsoluteUrl")` возвращал `undefined`.

|ERR-3467 (SUP-7217)
|Возникала ошибка `Input string was not in a correct format` при применении фильтра в представлении.

|ERR-3409 (SUP-7308)
|Кнопка печати не отображалась в задании `На исполнение` в ЭУ `_Предпросмотр файла_`.

|ERR-3398 (SUP-7294)
|Отсутствовала возможность не показывать предложение `Лист согласования документа содержит записи от предыдущего согласования, удалить их?` при открытии листа согласования.

|ERR-3259 (SUP-7234)
|В новом гриде не отображалось содержимое папок-ссылок.

|ERR-3231
|При первичной простой настройке пула Консоль настройки {wc}а непредвиденно завершалась с ошибкой.

|ERR-3174 (ERR-3174)
|В документации отсутствовала информация о том что не все файлы `.xlsm` могут отображаться в предварительном просмотре.

|ERR-3165 (SUP-7152)
|После редактирования сотрудника в Справочнике сотрудников в {wc}е, сотрудник удалялся из групп в которых он находился.

|ERR-3146 (SUP-7099)
|Файл формата `.xlsm` отображался пустым в окне предварительного просмотра.

|ERR-3143 (SUP-6665)
|В заданиях на консолидацию при отмене установки утилиты DVWebTool наблюдался бесконечный пинг.

|ERR-3119 (SUP-7099)
|Файлы с вкладки _Ход согласования_ не отображались в предварительном просмотре.

|ERR-3110 (SUP-7118)
|В предварительном просмотре файлы типа `.htm` отображались с некорректной кодировкой.

|ERR-3063
|Отсутствовала возможность удалить аватар сотрудника в Справочнике сотрудников {wc}а.

|ERR-2990 (SUP-7030)
|Исправлена ошибка, из-за которой циклы согласования отображались не по порядку, а также при переключении между циклами, они добавлялись.

|ERR-2938 (SUP-6946)
|В ЭУ `_Контейнер локации_` не отображались ссылки на строки.

|ERR-2933 (SUP-6975)
|Исправлена ошибка, при которой у исполнителей не очищались поля _Длительность_ и _Срок исполнения_.

|ERR-2923 (SUP-6925)
|Время исполнения группы заданий для каждого исполнителя не сохранялось, если не отправлять группу заданий на исполнение.

|ERR-2914 (SUP-6957)
|ЭУ в режиме редактирования _Редактирование_ в собственных типах карточек генерирует лишнюю строку с номером карточки.

|ERR-2895 (SUP-6928)
|Фильтрация значений для полей типа `Decimal` не работала со значениями больше 7 значимых цифр.

|ERR-2894 (SUP-6665)
|Настройка завершения работы утилиты DVWebTool после автоконсолидации не работала, если автоконсолидация завершалась с ошибкой.

|ERR-2879 (SUP-6944)
|При выполнении автоконсолидации возникала ошибка `HTTP error: status code 404`.

|ERR-2858 (SUP-6923)
|В документации была указана неверная сигнатура делегата `DescriptionColumnGeneratorDelegate`.

|ERR-2852 (SUP-6920)
|После входа пользователя в {wc} создавались три сессии, из которых только две удалялись после выхода.

|ERR-2822 (SUP-6837)
|Возникала ошибка `Cannot read properties of undefined reading data` при просмотре списка пользователей в справочнике сотрудников.

|ERR-2818 (SUP-6895)
|Возникала ошибка `A public action method GetAsCurrentVersion was not found on controller` при копировании версии согласующего в итоговую на этапе консолидации.

|ERR-2778 (SUP-6857)
|Не отображалась информация об обязательности заполнения ЭУ `_Сотрудниики_` или `_Записи справочника сотрудников_` в режиме редактирования по месту.

|ERR-2765 (SUP-6843)
|Возникала ошибка `Значение не может быть неопределенным. Имя параметра: value` при переходе к следующему или предыдущему значению в конструкторе справочников при помощи стрелочек.

|ERR-2745 (SUP-6810)
|В документации отсутствовало уточнение об ограничении на размер добавляемого файла.

|ERR-2742 (SUP-6815)
|Возникала ошибка `SQLFailure` при запуске согласования с условием старта и поиском по названию подразделения адресата.

|ERR-2737 (SUP-6754)
|Если в названии организации присутствовали кавычки, при поиске название организации отображалось как `\&quot;НазваниеОрганизации\&quot;`

|ERR-2715 (SUP-6550)
|Было невозможно отредактировать этап при запуске согласования, если в настройке этапа была установлена настройка `*Использовать значение из поля карточки*`.

|ERR-2696 (SUP-6779)
|После входа в {wc} с устройства на iOS стартовая страница отображалась только после смены ориентация экрана.

|ERR-2648 (SUP-6755)
|Требования к версии NodeJS в документации и в репозитории примеров на GitHub не совпадали.

|ERR-2647 (SUP-6754)
|Сортировка в ЭУ `_Записи справочника сотрудников_` осуществлялась в порядке "Имя, Отчество, Фамилия" вместо "Фамилия, Имя, Отчество".

|ERR-2617 (SUP-6731)
|Исправлена ошибка, при которой заместителю, назначенному через {wc}, не приходили задания замещаемого.

|ERR-2605 (SUP-6730)
|В ЭУ `_Число_` при нажатии kbd:[Enter] заменялось значение.

|ERR-2600 (SUP-6714)
|Не работало свойство `*Видимость*` для ЭУ `_Папка / Результаты поиска_`

|ERR-2581 (SUP-6698)
|Дополнительный файл не отображался в области предварительного просмотра в заданиях на согласование.

|ERR-2576 (SUP-6675)
|Информация о смене состояния сотрудника в документации была неполной.

|ERR-2559 (SUP-6686)
|Не работала настройка `*Обязательное*` в ЭУ `_Файл_`.

|ERR-2552 (SUP-6681)
|В документации отсутствовала информация о том, что утилита DVWebTool в одной ОС одновременно может быть запущена только от одного пользователя.

|ERR-2550 (SUP-6683)
|При выводе в представление поля с настройкой `*Замена в строке*`, настройка игнорировалась.

|ERR-2514 (SUP-6660)
|Во время обновления БД после обновления серверного расширения {wc}а возникала ошибка `Время ожидания истекло до завершения операции, или сервер не отвечает`.

|ERR-2493 (SUP-6646)
|В карточках документов и заданий при попытке открыть ход согласования возникала ошибка `Ссылка на объект не указывает на экземпляр объекта`.

|ERR-2489 (SUP-6643)
|В новом гриде в поле типа _bool_ значение _Да_ отображалось как _Даtrue_.

|ERR-2464 (SUP-6613)
|В ЭУ `_Записи справочника сотрудников_` возникала ошибка `SQLFailure` при использовании поиска.

|ERR-2460 (SUP-6607)
|Отсутствовала возможность вставить скопированную разметку _DV.Folder_.

|ERR-2440 (SUP-6578)
|Со временем начинала некорректно отображаться разметка окна поиска у настроенной пользовательской папки.

|ERR-2418 (SUP-6543)
|Возникала ошибка при открытии задания на согласование, если в ЭУ `*Файловая панель*` имелся архив.

|ERR-2390 (SUP-6570)
|В ходе согласования комментарий от делегата отображался у делегировавшего сотрудника.

|ERR-2379 (SUP-6552)
|При создании группы заданий из документа, на разметке создания группы заданий отображались ссылки только на основные файлы.

|ERR-2370 (SUP-6498)
|Возникала ошибка `Cast is not valid` при запуске согласования с условиями маршрута.

|ERR-2355 (SUP-6527)
|При согласовании в блоке _Комментарий к решению_ не сохранялись комментарии.

|ERR-2351 (SUP-6484)
|Не работала проверка уникальности при добавлении поля типа `Decimal`.

|ERR-2346 (SUP-6521)
|При делегировании задания с возвратом у делегата отсутствовала иконка решения.

|ERR-2338 (SUP-6503)
|Когда сотрудник из дочерней организации выбирал согласующих из ЭУ `_Записи справочника сотрудников_`, возникала ошибка `Ссылка на объект не указывает на экземпляр объекта`.

|ERR-2336 (SUP-6343)
|При копировании разметки из одного документа в другой сбрасывалась `*Операция редактирования*`.

|ERR-2333 (SUP-6509)
|Возникала ошибка `:Message: Cannot access URL, object not found` при открытии папки со ссылкой на URL в браузере Google Chrome.

|ERR-2307 (SUP-6360)
|При последовательном согласовании на этап консолидации приходила неправильная версия файла.

|ERR-2295 (SUP-6332)
|Не подсвечивались строки в представлении, если в папке использовался фон отличный от стандартного.

|ERR-2287 (SUP-6470)
|При создании группы заданий исполнители в результатах поиска не сортировались по алфавиту.

|ERR-2279 (SUP-6458)
|Исправлена ошибка, при которой этапы согласования, помеченные пропущенными, не пропускались после запуска маршрута.

|ERR-2273 (SUP-6367)
|Длительность в задании на исполнение рассчитывалась без учета бизнес-календаря подразделения.

|ERR-2249 (SUP-6440)
|При применении группировки по скрытой колонке возникала ошибка `Index is out of range`.

|ERR-2222 (SUP-6456)
|Текст метки ЭУ переносился на новую строку справа от метки.

|ERR-2217
|Исправлена ошибка, при которой не обновлялись счетчики непрочитанных карточек для папок замещения.

|ERR-2188 (SUP-6407)
|Если на событие было назначено несколько скриптов и хотя бы один из них не был найден, остальные скрипты не запускались.

|ERR-2183 (SUP-6415)
|В заданиях не отображалась ссылка на основной документ, если они были созданы из другого задания.

|ERR-2146 (SUP-6312)
|Текст должности сотрудника в этапе согласования выходил за рамки при подтверждении маршрута.

|ERR-2117 (SUP-6362)
|Возникала ошибка `Ссылка на объект не указывает на экземпляр объекта` при попытке открыть карточку, содержащую комментарий, сделанный удалённым сотрудником.

|ERR-2111 (SUP-6300)
|Во время конвертации файлов при предварительном просмотре в {wc} отсутствовала возможность пропускать все внешние ресурсы.

|ERR-2102 (SUP-6300)
|`IFileConvertationService` не поддерживал добавление собственных конвертеров.

|ERR-2095 (SUP-6356)
|Не срабатывало условие отображения разметки _Корень WebFrame_, заданное для роли.

|ERR-2086 (SUP-6351)
|Предпросмотр разных версий файла в ЭУ `*Список файлов*` показывал только первую версию документа.

|ERR-2085 (SUP-6349)
|В старом гриде для группировки представления не работала заданная настройка `*Показывать раскрытой*`.

|ERR-2073 (SUP-6333)
|При сортировке папок в боковом меню папки с вложениями имели приоритет при сортировке.

|ERR-2071 (SUP-6336)
|Исправлена ошибка, когда цвет кнопки журнала подписей не изменялся на голубой после подписания.

|ERR-2060 (SUP-6177)
|В справочнике сотрудников {wc}а не отображались настроенные колонки _Отображаемых полей сотрудников_: _ФИО_, _Фамилия_, _Имя_, _Отчество_.

|ERR-2053 (SUP-6329)
|Значение элемента управления `*Флажок*` в режиме редактирования сохранялось после изменения значения.

|ERR-2036 (SUP-6319)
|Выравнивание по правому краю, заданное в {kvr} не учитывалось при просмотре разметки.

|ERR-2030 (SUP-6314)
|В задании на согласование возникала ошибка `Uncaught TypeError: Cannot read properties of null (reading 'textContent')` при наличии среди основных файлов файла, недоступного для предпросмотра.

|ERR-2028 (SUP-6312)
|При настройке этапа _Согласование_, текст должности одного сотрудника накладывался на текст должности другого в случаях, когда было настроено собственное отображение сотрудников.

|ERR-2026 (SUP-6318)
|В ЭУ `_Текст_` режиме редактирования по месту с использованием HTML редактора возникала ошибка после отключения стиля начертания: `Uncaught IndexSizeError: Failed to execute 'setStart' on 'Range': The offset X is larger than the node's length (Y)`

|ERR-2003 (SUP-6286)
|Невозможно было менять параметры этапа согласования, если согласующие вычислялись при помощи БП.

|ERR-1954 (SUP-6274)
|После переноса сотрудника в другое подразделение возникала ошибка `Ссылка на объект не указывает на экземпляр объекта` при открытии {wc}а.

|ERR-1950 (SUP-6273)
|В режиме предварительного просмотра в заданиях дополнительный файл не отображался.

|ERR-1926 (SUP-6248)
|В предварительном просмотре вместо основного отображался дополнительный файл.

|ERR-1915 (SUP-6146)
|Исправлена ошибка, при которой отображаемый в превью файл после загрузки разметки изменялся.

|ERR-1912 (SUP-6146)
|В ЭУ `*Предпросмотр файла*` отображался файл, добавленный через скрытый ЭУ `*Список файлов*`.

|ERR-1880 (SUP-6234)
|Исправлена ошибка, когда в ЭУ `_Записи справочника сотрудников_` можно было выбрать последовательно два значения и каждое сохранялось в БД.

|ERR-1866 (SUP-6222)
|Возникала ошибка `Страница не найдена` при последующих переходах по ссылке в `_ЭУ Ссылка/Локация_`.

|ERR-1842 (SUP-6202)
|Исправлена ошибка, при которой не работала функция группировки в новом гриде.

|ERR-1840 (SUP-6192)
|В ЭУ `_Ход согласования_` не открывался предварительный просмотр файла в формате `.xlsx`.

|ERR-1829 (SUP-6191)
|Не отображалось задание в таблице исполнения документа, если документ был добавлен в архив.

|ERR-1807 (SUP-6029)
|Номер, сгенерированный из ЭУ `*Нумератор*` не освобождался после удаления.

|ERR-1795 (SUP-6043)
|При попытке сохранить файл, отредактированный через WebDAV, не выполнялась отправка на сервер, временный файл записывался на диск C.

|ERR-1757 (SUP-6127)
|При поиске сотрудника в ЭУ `_Записи справочника сотрудников_` возникала `Ошибка при разборе EntityName`, если в имени его организации присутствовал символ `&`.

|ERR-1750 (SUP-5938)
|Исправлена ошибка, когда в задании на консолидацию консолидатор мог открыть версию файла согласующего на редактирование.

|ERR-1746 (SUP-6126)
|Исправлена ошибка, когда текст значения ЭУ `_Флажок_` отображался ниже текста метки.

|ERR-1736 (SUP-5863)
|При очистке значения или нажатии на незаполненное поле ЭУ `_Дата/время_` возникала ошибка рендеринга.

|ERR-1728 (SUP-6114)
|При открытии файла на редактирование отображался диалог "Возможно внесённые изменения не сохранятся".

|ERR-1694 (SUP-6090)
|В ЭУ `_Файл_` было возможно удалить файл даже в режиме редактирования "Без редактирования".

|ERR-1682 (SUP-6085)
|Значение в `_Раскрывающийся список_` изменялось при каждом сохранении карточки.

|ERR-1673 (SUP-6059)
|Если при параметрическом поиске _Срок исполнения_ был установлен в значение *_Сейчас_*, время в полях выбора дат отображалось время из кэша -- с отставанием.

|ERR-1669 (SUP-5932)
|Возникала ошибка `Не удалось создать или открыть карточку строки справочника` при добавлении ссылки на файл в ЭУ `_Подразделение контрагента_`.

|ERR-1651 (SUP-5863)
|После добавления новой версии файла в задании на согласование отображалась старая версия файла до обновления страницы.

|ERR-1639 (SUP-6048)
|При отмене создания задания _На исполнение_ возникала ошибка `Internal Server Error`.

|ERR-1626 (SUP-5993)
|В ЭУ `_Ссылки_` добавлялось значение удалённой ссылки. Значение добавлялось, если удалить ссылку, открыть окно выбора карточки, затем закрыть кнопкой *ОК*, не выбирая карточку.

|ERR-1622 (SUP-6038)
|Исправлена ошибка, когда не высвобождался предыдущий свободный номер в ЭУ `_Нумератор_`.

|ERR-1591 (SUP-5982)
|Файл замечаний не отображался, если прикрепить файл не в боковой панели, а из самого задания завершить задание на согласование с решением _Отказано_.

|ERR-1572 (SUP-6004)
|При переключении на новый или старый грид менялся формат даты в виджете _Последние карточки_.

|ERR-1566 (SUP-6029)
|Возникала ошибка `Internal Server Error` при попытке сгенерировать новый номер для ЭУ `_Нумератор_`, привязанного к альтернативному источнику данных.

|ERR-1532 (SUP-5623)
|Папки, отключенные в боковой панели, продолжали отображаться после обновления страницы.

|ERR-1529 (SUP-6005)
|Возникала ошибка при попытке добавить через ЭУ `_Файл_`.

|ERR-1524 (SUP-6000)
|Некорректно работал ЭУ `_Записи справочника сотрудников_` в режиме выбора только сотрудников: очищалось поле _Согласующие_ при выборе подразделения, ранее выбранные сотрудники добавлялись после очистки поля.

|ERR-1521 (SUP-5989)
|При открытии конструктора справочников не отображались дочерние узлы, когда в свойствах выбрана настройка `*Область выбора*` *_Выбранный и дочерние к нему_*.

|ERR-1512 (SUP-5987)
|В свойстве `_Добавить ссылку_` ЭУ `_Ссылки_` не сохранялась добавленная операция редактирования.

|ERR-1481 (SUP-5724)
|Не изменялась версия приложенного файла после редактирования.

|ERR-1449 (SUP-5947)
|В виртуальной папке-делегате не отображалась форма с поисковыми запросами.

|ERR-1436 (SUP-5851)
|Возникала ошибка в ЭУ `_Предпросмотр файла_` при смене состояния задания.

|ERR-1421 (SUP-5930)
|При нажатии на ЭУ `_Записи справочника сотрудников_` возникала ошибка `Ссылка не указывает на экземпляр объекта`.

|ERR-1419 (SUP-5926)
|Не срабатывало условие выбора маршрута по сумме договора.

|ERR-1412 (SUP-5922)
|Фильтрация данных работала только для представления по умолчанию.

|ERR-1402 (SUP-5759)
|Исправлена ошибка, из-за которой счетчики непрочитанных карточек переставали работать после восстановления подключения.

|ERR-1400 (SUP-5903)
|Длинные строки в ЭУ `_Текст_` не переносились в режиме редактирования по месту.

|ERR-1398 (SUP-5893)
|Изменение полей в документе, встроенном в задание через `*Контейнер*`, блокировало изменения в задании.

|ERR-1379 (SUP-5884)
|В конфигурационном файле {wc}а отсутствовала ссылка на конкретную библиотеку NLog.

|ERR-1360 (SUP-5889)
|После удаления ссылки в ЭУ и последующем выборе пустой ссылки в `*Ссылка на карточку*` добавлялось значение удаленной ссылки.

|ERR-1356 (SUP-5885)
|При открытии карточки в режиме _редактирование_ автоматически срабатывала прокрутка до ЭУ `*Текст*`, если для него было включено использование HTML редактора.

|ERR-1297 (SUP-5833)
|В дереве исполнения при нажатии и удержании кнопки мыши появлялась ошибка `Uncaught TypeError: Cannot read properties of undefined (reading 'services')`.

|ERR-1290 (SUP-5822)
|В поле атрибутивного поиска `Автор = Я` отображался не текущий пользователь, а предыдущий пользователь, который выполнял вход в {wc}.

|ERR-1286 (SUP-5808)
|При отмене согласования в истории карточки отсутствовала соответствующая запись.

|ERR-1276 (SUP-5562)
|При копировании разметки в другое решение частично сбрасывались операции редактирования.

|ERR-1264 (SUP-5813)
|Не отображался штамп подписи при предварительном просмотре файла в {wc}е версии 10.

|ERR-1261 (SUP-5808)
|В истории карточки не фиксировалась операция *Создать согласование*.

|ERR-1260 (SUP-5800)
|Исправлена ошибка, при которой в Справочнике сотрудников нельзя было создать сотрудника или отредактировать поля у уже созданного.

|ERR-1259 (SUP-5796)
|После возврата из карточки к результатам параметрического поиска не сохранялись последние результаты поиска.

|ERR-1251 (SUP-5750)
|Возникала ошибка `Элемент с указанным ключом не найден в коллекции` при открытии карточки задания на исполнение.

|ERR-1230 (SUP-5757)
|Когда в разметке присутствовало два ЭУ `_Записи справочника сотрудников_`, сохранялось значение только второго из ЭУ.

|ERR-1222 (SUP-5741)
|Отображалась пустая страница при нажатии кнопки *Назад* из карточки, если перед этим переключались файлы в ЭУ `*Предпросмотр файла*`.

|ERR-1219 (SUP-5719)
|После перехода из папки в боковом меню в другой пункт меню, счетчик непрочитанных карточек сбивался на предыдущее значение.

|ERR-1218 (SUP-5724)
|Когда УЗ пользователя {wc}а отличалась от УЗ пользователя на компьютере, где открывался Word, вместо новой версии файла отображалась старая.

|ERR-1203 (SUP-5749)
|Не сохранялось значение нумератора при вводе регистрационного номера с двумя слешами.

|ERR-1188 (SUP-5760)
|В окне создания ссылки на существующую карточку не отображались виртуальные папки.

|ERR-1164 (SUP-5678)
|В режиме редактирования по месту не был виден переход по kbd:[Tab] на элемент управления в разметке просмотра.

|ERR-1159 (SUP-5719)
|При открытии непрочитанной карточки в новом гриде счетчик непрочитанных карточек в папке не уменьшался.

|ERR-1153 (SUP-5709)
|При сохранении карточки документа возникала ошибка `Internal exception, error code: InvalidRow`.

|ERR-1089 (SUP-5700)
|При расчете срока исполнения группы заданий по бизнес календарю не учитывались праздничные дни.

|ERR-1086 (SUP-5702)
|Если в старом гриде поиск выполнялся с главной страницы, открывалась папка результатов поиска без результатов поиска.

|ERR-1078 (SUP-5695)
|Возникала ошибка открытия версии файла в задании на согласование если в разметке присутствовал ЭУ `*Список файлов*`.

|ERR-1073 (SUP-5696)
|Исправлена ошибка, из-за которой по REST API не закрывались сессии {wc}а.

|ERR-1070 (SUP-5683)
|Создавалась лишняя сессия администратора при авторизации в {wc}е или запуске программы {kvr}.

|ERR-1069 (SUP-5683)
|После закрытия программы {kvr} оставались занятые сессии и не освобождались лицензии.

|ERR-1067 (SUP-5686)
|Событие `*Перед сохранением карточки*` вызывалось позднее задуманного, что не позволяло выполнить код до проверки обязательных полей при сохранении карточки.

|ERR-1057 (SUP-5678)
|Исправлена ошибка, при которой не выделялась строка ЭУ `_Раскрывающийся список_` при переключении по kbd:[Tab].

|ERR-1055 (SUP-5682)
|Исправлена ошибка, при которой вместо результатов поиска отображалась пустая страница.

|ERR-1050 (SUP-5623)
|Возникала ошибка при открытии окна настроек прав доступа для _Справочника сотрудников_.

|ERR-1027 (SUP-5655)
|Итоговая версия документа, загруженная из файла на этапе консолидации не была видна на следующем этапе.

|ERR-1026 (SUP-5630)
|При размещении ЭУ `*Файл*` в таблице отсутствовала кнопка удаления файла.

|ERR-1022 (SUP-5657)
|В ЭУ `*Сотрудник*` и `*Сотрудники*` не раскрывался список сотрудников при повторном нажатии на иконку *Показать варианты*.

|ERR-1001 (SUP-5660)
|Некорректно отображалась кодировка текстовых файлов в формате `.xml` при предварительном просмотре.

|ERR-988 (SUP-5595)
|Исправлена ошибка, возникавшая при сохранении карточек задания в разметке просмотра из скрипта.

|ERR-987 (SUP-5608)
|Если при подписании отсутствовала простая электронная подпись, не раскрывался список сертификатов.

|ERR-966 (SUP-5609)
|Для ЭУ `*Ссылки*` не было возможности отфильтровать ссылки разных типов.

|ERR-965 (SUP-5613)
|В браузере Internet Explorer не отображался ЭУ `_Текст_`, если было активировано свойство `*Использовать HTML редактор*`.

|ERR-961 (SUP-5581)
|В ЭУ `*Записи справочника  сотрудников*` не всегда показывались избранные сотрудники при установке курсора в поле ввода.

|ERR-960 (SUP-5603)
|Не изменялось представление в {wc}е версии 14 после его изменения в {wincl}е.

|ERR-947 (SUP-5664)
|Если в карточке отсутствовал файл, в превью файла отображался файл из последней открытой карточки.

|ERR-921 (SUP-5582)
|После ухода сессии в офлайн, вторая сессия {wc}а продолжала оставаться активной и выполняла расчёт счетчиков.

|ERR-919 (SUP-5586)
|При попытке установить программу {kvr} отдельно от сервера {wc}а, работа мастера установки завершалась преждевременно с ошибкой.

|ERR-909 (SUP-5543)
|Исправлена ошибка с неправильным порядком этапов в ходе согласования, когда последний этап отображался как первый.

|ERR-906 (SUP-5557)
|При отмене атрибутивного поиска после долгой загрузки страницы происходило перенаправление предыдущую папку.

|ERR-904 (SUP-5571)
|Если у столбца типа *_Дата и время_* был указан *_Средний формат даты_*, в Excel представление экспортировалось в формате отличном от отображаемого в представлении.

|ERR-903 (SUP-5571)
|При экспорте представления в Excel, столбец типа _Да/Нет_ экспортировался как `True/`.

|ERR-901 (SUP-5565)
|Исправлена ошибка, при которой не применялась сортировка в поисковых папках в старом гриде.

|ERR-899 (SUP-5328)
|При изменении версии файла на согласовании каждый раз создавалась новая версия.

|ERR-890 (SUP-5570)
|В документации на сайте https://docsvision.com/docs/[docsvision.com] были указаны нерелевантные требования к версии платформы со стороны {wc}а 16 и модуля Управление архивом.

|ERR-840 (SUP-5507)
|Свойство `*Ручной ввод разрешён*` в ЭУ `_Нумератор_` было недоступным для чтения и записи при помощи пользовательского скрипта.

|ERR-834 (SUP-5351)
|Не добавлялась метка времени при подписании усиленной подписью в задании на подписание.

|ERR-831 (SUP-5503)
|Не получалось сгенерировать новый номер или очистить старый у ЭУ `_Нумератор_` в разметке редактирования, если режим редактирования ЭУ: Редактирование.

|ERR-804 (SUP-5485)
|После экспорта представления из {wc} в Excel в конце строк экспортированного файла добавлялся пробел.

|ERR-796 (SUP-3330)
|При поиске сотрудника в ЭУ `_Записи справочника сотрудников_` после повторного нажатия на кнопку *Показать ещё* результаты поиска дублировались.

|ERR-788 (SUP-1864)
|При установке флага `*Не показывать при выборе в карточке*` сотрудник переставал отображаться в поиске.

|ERR-762 (SUP-5570)
|При создании карточки с несколькими незаполненными обязательными полями выдавалось пустое сообщение об ошибке.

|ERR-759 (SUP-5437)
|В элементах управления с настройкой `*Использовать последних*` был доступен выбор значения при неразрешённой операция редактирования

|ERR-758 (SUP-5436)
|При печати ЭУ `_Предпросмотр_` печатался лишний лист.

|ERR-699 (SUP-5034)
|Исправлена ошибка, при котрой после установки Office toolbar не работал почтовый клиент Outlook.

|ERR-698 (SUP-4529)
|Исправлена ошибка, когда в меню "Установка и удаление программ" названия модулей ЭДО отображались нелокализованными.

|ERR-336 (SUP-5328)
|После пройденного согласования файл, приложенный к карточке документа, не содержал правок, созданных на этапе консолидации.

|ERR-327 (SUP-5368)
|Возникала ошибка `Не удалось получить свойство "system" ссылки, значение которой неопределено или является NULL` при добавлении расширения грида.

|ERR-321 (SUP-5406)
|При запуске согласования не срабатывало условие старта маршрута согласования.

|ERR-317 (SUP-5374)
|При наведении курсора подсказка на элемент управления подсказка появлялась в разных местах, отличных от месторасположения курсора.

|ERR-307 (SUP-5382)
|ЭУ `_Текст_` не отображался Internet Explorer при включённом режиме HTML редактора.

|ERR-288 (SUP-5421)
|Не работала кнопка "Обновить" на панели управления грида после удаления ярылка карточки image:user:buttons/refresh-black.png[Обновить] в представлении Дайджест.

|ERR-286 (SUP-5431)
|Не отображался комментарий к решению в задании и в ЭУ `_Ход согласования_` в связанном документе.

|ERR-244 (SUP-5363)
|Некорректно работал атрибутивный поиск при использовании БД PostgreSQL: чтобы найти карточки по дате, требовалось ввести предыдущий день.
|===
